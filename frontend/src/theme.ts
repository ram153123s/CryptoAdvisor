import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: { main: "#f7931a" },
    background: { default: "#1b1f27", paper: "#65686e" },
  },
});

export default theme;
